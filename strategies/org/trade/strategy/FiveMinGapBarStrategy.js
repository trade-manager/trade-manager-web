function runStrategy(candleSeries, newBar) {

  try {
  let candleSeries = JSON.parse(candleSeries);


  // Get the current candle
  //CandleItem currentCandleItem = this.getCurrentCandle();
  } catch (ex) {

  }
}

